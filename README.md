# Civis

Civis is a people management application with face recognition features. 
It can be deployed using Docker.

## How to run with Docker

```bash
docker-compose up -d
```

Access the client at http://localhost:10000 and the server API at http://localhost:5005.
